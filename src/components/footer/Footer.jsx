import React from 'react';
import { StyledFooter } from './Footer.styled';

const Footer = () => {
    return (
        <StyledFooter>Made with love by Estela Franco</StyledFooter>
    );
};

export default Footer;